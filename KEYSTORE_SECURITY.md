# ⚠️ ВАЖНО: Безопасность Keystore

## Созданные файлы

✅ **Keystore файл**: `app/keystore/release-key.jks`
✅ **Конфигурация**: `keystore.properties`

## 🔐 Пароли Keystore

**ВАЖНО: Сохраните эти пароли в безопасном месте! Без них вы не сможете обновлять приложение в Google Play.**

### Store Password (пароль хранилища):
```
BfuwAvsOjcqYYBsWIAC3vpjKJ
```

### Key Password (пароль ключа):
```
jFdxdZxhBi3T9L2m1OWVyMnBq
```

### Key Alias:
```
upload-key
```

## 📝 Рекомендации по безопасности

1. **Сохраните пароли в менеджере паролей** (1Password, LastPass, Bitwarden и т.д.)
2. **Создайте резервную копию keystore файла** в безопасном месте (зашифрованный архив)
3. **НЕ коммитьте** `keystore.properties` и `release-key.jks` в git (они уже в .gitignore)
4. **НЕ делитесь** паролями с кем-либо, кроме доверенных разработчиков

## 🚀 Использование

Теперь вы можете собрать release версию приложения:

```bash
./gradlew assembleRelease
```

или

```bash
./gradlew bundleRelease
```

## ⚠️ Если вы потеряете keystore или пароли

- **НЕВОЗМОЖНО** восстановить доступ к существующему приложению в Google Play
- Придется создавать новое приложение с новым package name
- Все пользователи должны будут переустановить приложение

## 🔄 Миграция в PKCS12 (опционально)

Keytool рекомендует мигрировать в PKCS12 формат. Это можно сделать командой:

```bash
keytool -importkeystore -srckeystore app/keystore/release-key.jks \
  -destkeystore app/keystore/release-key.jks -deststoretype pkcs12 \
  -srcstorepass 'BfuwAvsOjcqYYBsWIAC3vpjKJ' \
  -deststorepass 'BfuwAvsOjcqYYBsWIAC3vpjKJ'
```

Но текущий JKS формат также работает нормально.





















