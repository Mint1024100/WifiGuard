# Инструкция по сборке релизной версии WifiGuard

## 📋 Предварительные требования

1. **JDK 17 или выше** установлен и настроен
2. **Android SDK** установлен (compileSdk 35)
3. **Keystore файл** создан и настроен
4. **Gradle** настроен корректно

## 🔐 Настройка Keystore (первый раз)

### 1. Создание keystore файла

```bash
keytool -genkey -v -keystore app/keystore/release-key.jks \
  -alias upload-key \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000
```

**Важно**: 
- Сохраните пароли в безопасном месте (менеджер паролей)
- Создайте backup keystore файла
- Без keystore невозможно обновлять приложение в Google Play!

### 2. Настройка keystore.properties

Создайте файл `keystore.properties` в корне проекта (НЕ коммитьте его!):

```properties
storeFile=app/keystore/release-key.jks
storePassword=ВАШ_ПАРОЛЬ_КЕЙСТОРА
keyAlias=upload-key
keyPassword=ВАШ_ПАРОЛЬ_КЛЮЧА
```

Убедитесь, что `keystore.properties` в `.gitignore`:

```bash
grep -q "keystore.properties" .gitignore || echo "keystore.properties" >> .gitignore
```

## 🏗️ Сборка релизной версии

### Вариант 1: Сборка APK (для тестирования)

```bash
./gradlew clean assembleRelease
```

APK будет находиться в: `app/build/outputs/apk/release/app-release.apk`

### Вариант 2: Сборка AAB (для Google Play)

```bash
./gradlew clean bundleRelease
```

AAB будет находиться в: `app/build/outputs/bundle/release/app-release.aab`

**Рекомендуется**: Использовать AAB для публикации в Google Play, так как Google Play оптимизирует размер для каждого устройства.

## ✅ Проверка подписи

### Проверка APK

```bash
jarsigner -verify -verbose -certs app/build/outputs/apk/release/app-release.apk
```

### Проверка AAB

```bash
jarsigner -verify -verbose -certs app/build/outputs/bundle/release/app-release.aab
```

### Проверка информации о подписи

```bash
# Для APK
apksigner verify --print-certs app/build/outputs/apk/release/app-release.apk

# Для AAB (используйте bundletool)
bundletool verify --bundle=app/build/outputs/bundle/release/app-release.aab
```

## 📊 Анализ размера

### Анализ размера AAB

```bash
./gradlew analyzeReleaseBundle
```

Результаты будут в: `app/build/outputs/bundle/release/`

### Анализ размера APK

```bash
./gradlew analyzeReleaseApk
```

## 🧪 Тестирование релизной сборки

### 1. Установка на устройство

```bash
# Для APK
adb install -r app/build/outputs/apk/release/app-release.apk

# Для AAB (сначала конвертируйте в APK)
bundletool build-apks --bundle=app/build/outputs/bundle/release/app-release.aab \
  --output=app-release.apks \
  --ks=app/keystore/release-key.jks \
  --ks-pass=pass:ВАШ_ПАРОЛЬ \
  --ks-key-alias=upload-key \
  --key-pass=pass:ВАШ_ПАРОЛЬ_КЛЮЧА

bundletool install-apks --apks=app-release.apks
```

### 2. Проверка функциональности

- [ ] Приложение запускается
- [ ] Wi-Fi сканирование работает
- [ ] Разрешения запрашиваются корректно
- [ ] Уведомления работают
- [ ] Фоновый мониторинг работает
- [ ] Нет debug логов в logcat (проверьте: `adb logcat | grep -i "wifiguard"`)

## 🚀 Публикация в Google Play

### 1. Подготовка материалов

Убедитесь, что все материалы готовы:
- [ ] Скриншоты (минимум 2, максимум 8)
- [ ] Иконка приложения (512x512)
- [ ] Feature graphic (1024x500)
- [ ] Описание приложения
- [ ] Краткое описание
- [ ] Политика конфиденциальности

### 2. Загрузка в Google Play Console

1. Войдите в [Google Play Console](https://play.google.com/console)
2. Выберите приложение WifiGuard
3. Перейдите в раздел **Production** (или **Internal testing** для тестирования)
4. Нажмите **Create new release**
5. Загрузите AAB файл
6. Заполните **Release notes**
7. Нажмите **Review release**

### 3. Проверка перед публикацией

- [ ] Pre-launch report пройден
- [ ] App Bundle Explorer проверен
- [ ] Data safety form заполнен
- [ ] Content rating получен
- [ ] Target audience указан

### 4. Публикация

1. Нажмите **Start rollout to Production**
2. Подтвердите публикацию
3. Ожидайте одобрения Google (обычно 1-7 дней)

## 🔄 Обновление версии

При подготовке следующего релиза:

1. **Обновите версию** в `gradle.properties`:
   ```properties
   APP_VERSION_NAME=1.1.1
   APP_VERSION_CODE=8
   ```

2. **Обновите changelog** в Google Play Console

3. **Соберите новую версию**:
   ```bash
   ./gradlew clean bundleRelease
   ```

4. **Загрузите в Google Play Console**

## ⚠️ Важные замечания

1. **Keystore backup**: Храните backup keystore в безопасном месте. Без него невозможно обновлять приложение!

2. **Version Code**: Должен увеличиваться с каждым релизом. Google Play не принимает версии с меньшим versionCode.

3. **Target SDK**: Должен соответствовать требованиям Google Play. Текущая версия: 35.

4. **Тестирование**: Всегда тестируйте релизную сборку на реальных устройствах перед публикацией.

5. **ProGuard**: Убедитесь, что все необходимые классы сохранены в ProGuard правилах.

## 🐛 Решение проблем

### Проблема: Keystore не найден

**Решение**: Убедитесь, что:
- Файл `keystore.properties` существует в корне проекта
- Путь к keystore файлу указан правильно
- Keystore файл существует по указанному пути

### Проблема: Ошибка подписи

**Решение**: 
- Проверьте правильность паролей в `keystore.properties`
- Убедитесь, что alias указан правильно
- Проверьте срок действия сертификата: `keytool -list -v -keystore app/keystore/release-key.jks`

### Проблема: ProGuard ошибки

**Решение**:
- Проверьте `app/proguard-rules.pro`
- Добавьте необходимые правила для проблемных классов
- Проверьте логи сборки для деталей

### Проблема: Большой размер APK/AAB

**Решение**:
- Используйте AAB вместо APK (Google Play оптимизирует размер)
- Проверьте неиспользуемые ресурсы
- Используйте `shrinkResources = true` (уже включено)
- Проверьте зависимости на дубликаты

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сборки: `./gradlew clean bundleRelease --stacktrace`
2. Проверьте документацию Android: https://developer.android.com/studio/publish
3. Проверьте требования Google Play: https://support.google.com/googleplay/android-developer

---

**Последнее обновление**: $(date)
**Версия документа**: 1.0






