# Миграция базы данных Room: версия 5 → 6

## Проблема
```
Room cannot verify the data integrity. 
Looks like you've changed schema but forgot to update the version number.

Expected hash: d25a81daab64a031f59ec63663fdfec0
Found hash: 3190673ce85057b9a3c0563c05881467
```

## Решение

### ✅ Выполненные изменения

1. **Увеличена версия базы данных**: `version = 5` → `version = 6`
   - Файл: `WifiGuardDatabase.kt`
   - Строка 41

2. **Создана миграция MIGRATION_5_6**
   - Добавлены недостающие индексы для таблицы `threats`:
     - `index_threats_isResolved` - для фильтрации активных угроз
     - `index_threats_scanId` - для связи с сканированиями
     - `index_threats_severity_isResolved` - составной индекс для частых запросов
     - `index_threats_isNotified` - для отслеживания уведомлений
     - `index_threats_networkBssid` - для связи с сетями
     - `index_threats_threatType` - для фильтрации по типу угрозы

3. **Добавлена миграция в цепочку миграций**
   - Файл: `WifiGuardDatabase.kt`
   - Строка 372: добавлен `MIGRATION_5_6` в список миграций

### 🔒 Соблюдение стандартов безопасности

**ВАЖНО**: Согласно `.cursorrules` проекта WifiGuard:
- ❌ **НЕ используется** `fallbackToDestructiveMigration()`
- ✅ **Создана безопасная миграция** с сохранением всех данных
- ✅ **Добавлена валидация** целостности данных после миграции
- ✅ **Добавлено логирование** для отслеживания процесса миграции

### 📊 Преимущества миграции

1. **Сохранение данных**: Все существующие данные пользователя сохранены
2. **Оптимизация производительности**: Новые индексы ускоряют запросы к таблице threats
3. **Безопасность**: Транзакционная миграция с откатом в случае ошибки
4. **Мониторинг**: Подробное логирование процесса миграции

### 🔍 Технические детали

**Использование `CREATE INDEX IF NOT EXISTS`**:
- Безопасное создание индексов
- Не вызывает ошибку, если индекс уже существует
- Идемпотентная операция

**Транзакционность**:
```kotlin
database.beginTransaction()
try {
    // SQL операции
    database.setTransactionSuccessful()
} finally {
    database.endTransaction()
}
```

**Валидация после миграции**:
- Проверка количества записей в таблицах
- Проверка целостности внешних ключей
- Логирование результатов

## Следующие шаги

1. Пересобрать проект: `./gradlew clean build`
2. Проверить, что новая схема (версия 6) экспортирована в `app/schemas/`
3. Протестировать миграцию на тестовом устройстве
4. Убедиться, что все данные сохранены после обновления

## Примечания

- Миграция выполняется автоматически при первом запуске приложения после обновления
- Пользователь не заметит процесс миграции (выполняется в фоне)
- В логах будут видны сообщения о начале и завершении миграции
- При ошибке миграции приложение не запустится (fail-fast подход для защиты данных)











